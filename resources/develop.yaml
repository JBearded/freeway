server:
  websocket:
    port: 8080
    readBufferSize: 1024
    writeBufferSize: 1024
    # allowOrigin: "^(http|https)://(\\w+)\\.github\\.com"
    allowOrigin: "(.*)"
  http:
    port: 8888
database:
  default:
    host: 127.0.0.1
logger:
  path: "/log/freeway/info.log"
  level: debug
